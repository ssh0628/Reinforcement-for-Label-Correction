[project]
name = "rlnlc"
version = "0.1.0"
description = "A compact CIFAR-10 implementation of reinforcement learning for noisy-label correction"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0.0",
    "scipy>=1.15.0",
    "torch>=2.4.0",
    "torchvision>=0.19.0",
]
